package com.codecool.shop.process;

public class CheckoutProcess extends Process {
    /*
    @Override
    protected void action (Orderable item) {
        item.checkout();
        System.out.println("Success! New status of the item with ID " + item.getId().toString() + " is: " + item.getStatus());
    }*/
}
